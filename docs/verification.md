# Проверка 2026-09-23

- 76 unittest-тестов прошли, включая весь существующий набор и 15 новых проверок наставника.
- Production-сборка Vite прошла (`pnpm build`, configLoader runner).
- Проверены скоринг, повторное завершение, импорт и ревизии, разграничение ролей,
  выдача/отзыв карточек, сигналы стагнации и локализация.
- Новые тесты: изоляция чата, запрет подмены ID/system-сообщений, отсутствие мутаций,
  три языка, ограничения ввода, лимит запросов, fallback, смена ревизии,
  mock HTTP-контракт Responses API и Unicode-токен (401 вместо исключения).
- В браузере с отдельной QA-базой проверены импорт четырёх файлов из
  examples/stagnation, фильтр риска, черновик предложения, вход сотрудника,
  объяснение System Design против Public Speaking, ответ на просьбу начислить
  Coins/зачесть обучение и казахский ответ наставника.
- Исправлена защита UI от применения устаревшего ответа профиля после выхода.

Одиночные запросы через TestClient, временная SQLite, синтетические 200 сотрудников,
40 событий, 60 навыков и 4800 записей истории:

| Endpoint | Секунды |
|---|---:|
| /api/me | 0.028 |
| /api/recommendations | 0.010 |
| /api/hr/summary | 0.104 |
| /api/hr/employees | 0.137 |

Это диагностический замер, не гарантия SLA, не нагрузочный тест и не скорость
отрисовки UI. Docker не проверен (нет установленного Docker). Реальный внешний
LLM не проверен (не настроен ключ); адаптер проверен через MockTransport.
Демо-завершение не заменяет подтверждение LMS. Официальный стартовый кит проверен на следующем этапе, см. ниже.

Повторить автоматические проверки:

```sh
python -m unittest discover -s tests -v
cd frontend
pnpm build
```

## Этап Halyk Store

89 тестов прошли; сборка Vite прошла. В браузере на отдельной store-qa.sqlite3:
начисление 100 за system-lab, предпросмотр обмена, списание до 0, история,
запрет покупки при нехватке Coins, семейная категория и казахский интерфейс.

## Запуск без Docker

96 тестов прошли. run.py --smoke собрал интерфейс, запустил серверы, проверил
API через proxy и остановил свои процессы. Внешний API не вызывался.


## Официальный кит

111 тестов прошли. Сборка React/Vite прошла. Полный импорт проверен через API
на отдельной временной SQLite; активный датасет пользователя не заменялся.
Тесты всех 200 пересчитанных профилей, валидации, роли/грейда/prerequisites,
повторного клуба, последнего обязательного назначения, нулевых Coins при импорте.
Кейс E0028 после пересчёта имеет System Design=3. Отдельный изменённый тестовый
профиль проверяет приоритет System Design над Speaking при трёх no_show.
Скорость одиночных API: me 0.011с, recommendations 0.012с, summary 0.057с,
hr/employees 0.217с. Внешняя LLM в проверках выключена.
